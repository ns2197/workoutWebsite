import React from 'react'
import Header from '../Header/Header'

function WorkoutVideos() {
  return (
    <div>
      <Header/>
        <p> 
          the page currently doesn't have any data

          Still working on it
        </p>
    </div>
  )
}

export default WorkoutVideos